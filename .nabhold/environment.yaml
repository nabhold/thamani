schema: { name: "nabhold-development-environment-contract", version: "1.0" }
application: { name: "thamani", version: "0.1.0", entity_id: "THAMANI-GLOBAL" }
environment:
  {
    provider: "baobab-dev",
    profile: "frontend",
    minimum_version: "1.2.6-frontend",
    contract_minimum_version: "1.0",
  }
compatibility_policy:
  {
    strict_runtime_versions: true,
    allow_newer_patch_versions: true,
    allow_newer_minor_versions: false,
    allow_older_versions: false,
  }
validation:
  required_capabilities:
    ["languages.node", "package_managers.pnpm", "development.github_cli", "frontend_tooling.turbo"]
  excluded_capabilities:
    - { name: "python", reason: "Frontend repository builds no Python code." }
    - { name: "flutter", reason: "Frontend repository builds no Flutter code." }
    - { name: "postgresql", reason: "The estate accesses no database directly." }
    - { name: "docker", reason: "Not required for ordinary Codespaces development." }
  pending_capabilities: []
notes:
  - "Codespaces uses ghcr.io/nabhold/baobab-dev:1.2.6-frontend."
  - "Browser-dependent CI uses the 1.2.6-frontend-e2e profile."
