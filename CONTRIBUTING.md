# Contributing

Use a short-lived branch and pull request. Keep the commerce/platform boundaries in `README.md`; changes that alter them require an ADR. Before review run `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build`. GitHub Actions must be pinned to full commit SHAs.
