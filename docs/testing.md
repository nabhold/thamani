# Testing

Vitest covers pure utilities and validation. React Testing Library is available for interactive components as they arrive. Playwright is configured for desktop Chromium and a mobile viewport; critical purchase E2E tests remain gated until Baobab Trade supplies deterministic seeded products, shipping and payment providers.

CI still builds the application on every pull request. It does not pretend an unavailable commerce backend constitutes a passing purchase flow.
